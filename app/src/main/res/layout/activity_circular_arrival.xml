<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/medical_background"
    android:padding="24dp"
    tools:context=".ui.main.CircularArrivalActivity">

    <!-- Top Navigation Bar (Visual only as elements are split in this view, relying on layout consistency) -->
    <!-- Actually, let's keep the split layout clean without a distinct top bar for arrival, similar to other Nav pages -->
    
    <!-- Robot Image: Left Side (40%) -->
    <ImageView
        android:id="@+id/iv_robot"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:contentDescription="Robot"
        android:scaleType="fitCenter"
        android:src="@drawable/qinglang"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/guideline_vertical_center"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintWidth_percent="0.40" />

    <!-- Vertical Guideline -->
    <androidx.constraintlayout.widget.Guideline
        android:id="@+id/guideline_vertical_center"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        app:layout_constraintGuide_percent="0.45" />

    <!-- Right Content Area -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="0dp"
        android:layout_height="match_parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@+id/guideline_vertical_center"
        app:layout_constraintTop_toTopOf="parent">

        <!-- Main Status -->
        <LinearLayout
            android:id="@+id/status_content"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:orientation="vertical"
            app:layout_constraintBottom_toTopOf="@+id/action_area"
            app:layout_constraintTop_toTopOf="parent">

            <TextView
                android:id="@+id/tv_current_point"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="当前点位: --"
                android:textColor="@color/medical_text_secondary"
                android:textSize="24sp" />

            <TextView
                android:id="@+id/tv_status_title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="24dp"
                android:text="已到达"
                android:textColor="@color/medical_primary"
                android:textSize="80sp"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/tv_arrival_message"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="48dp"
                android:gravity="center"
                android:lineSpacingExtra="10dp"
                android:text="请尽快把货物放入机器人中\n(无人操作将继续下一站: 30s)"
                android:textColor="@color/medical_text_primary"
                android:textSize="26sp" />

        </LinearLayout>

        <!-- Bottom Actions -->
        <LinearLayout
            android:id="@+id/action_area"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:gravity="center"
            android:paddingBottom="48dp"
            app:layout_constraintBottom_toBottomOf="parent">

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btn_open_door"
                android:layout_width="320dp"
                android:layout_height="72dp"
                android:layout_marginBottom="24dp"
                android:backgroundTint="@color/teal_700"
                android:text="手动开门"
                android:textColor="@color/white"
                android:textSize="24sp"
                app:cornerRadius="36dp"
                app:icon="@drawable/ic_door_open"
                app:iconGravity="textStart"
                app:iconTint="@color/white"
                app:iconSize="32dp" />

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:orientation="horizontal">

                <com.google.android.material.button.MaterialButton
                    android:id="@+id/btn_full_return"
                    android:layout_width="wrap_content"
                    android:layout_height="72dp"
                    android:layout_marginEnd="32dp"
                    android:paddingHorizontal="32dp"
                    android:backgroundTint="@color/medical_warning"
                    android:text="已装满 返航"
                    android:textColor="@color/white"
                    android:textSize="24sp"
                    app:cornerRadius="36dp"
                    app:icon="@drawable/ic_home_return"
                    app:iconGravity="textStart"
                    app:iconTint="@color/white"
                    app:iconSize="32dp" />

                <com.google.android.material.button.MaterialButton
                    android:id="@+id/btn_continue"
                    android:layout_width="wrap_content"
                    android:layout_height="72dp"
                    android:paddingHorizontal="32dp"
                    android:backgroundTint="@color/medical_primary"
                    android:text="关门并前往下一站"
                    android:textColor="@color/white"
                    android:textSize="24sp"
                    app:cornerRadius="36dp"
                    app:icon="@drawable/ic_play_circle"
                    app:iconGravity="textStart"
                    app:iconTint="@color/white"
                    app:iconSize="32dp" />
            </LinearLayout>
        </LinearLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
